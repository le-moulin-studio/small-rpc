package com.lemoulinstudio.small.apt.model;

/**
 * An interface to tag network interface models as been a view of
 * another network interface model of the opposite side (see {@link HostKind}).
 *
 * @author Vincent Cantin
 */
public interface View<T> {
}
