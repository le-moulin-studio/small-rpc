package com.lemoulinstudio.small.common;

/**
 * <p>An interface that is used to tag the "remote interfaces" generated
 * by the Small Annotation Processor.</p>
 *
 * <p>An object that implements "remote interface" is able to be exposed
 * on the network and be called from a another host if provided to the
 * Small session.</p>
 *
 * @author Vincent Cantin
 */
public interface RemoteService {
}
